package main

import (
	"fmt"
	"sort"
)

func main() {
	fmt.Println("vim-go")
}

func subsetsWithDup(nums []int) (ans [][]int) {
	ans = append(ans, []int{}, nums)
	sort.Ints(nums)
	l := len(nums)
	for i := 0; i < l; i++ {
		append
	}
	return ans
}
